# FalaOrçamento v0.8 — interpretação por IA

Base: v0.7 online + visual/parser aprovado da v0.6.3.

## Novo
- Endpoint `/api/interpret` no backend.
- Linguagem natural interpretada por IA.
- Saída estruturada: cliente, descrição, quantidade, unitário e observações.
- A chave da API nunca vai para o navegador.
- Interpretador local permanece como fallback.
- Sem mudança no fluxo visual aprovado.

Leia `CONFIGURAR_IA.md` para ativar a IA no Render.
